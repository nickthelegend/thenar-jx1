# JX1 unresolved issues (v0.5, 2026-09-24)

Status legend: OPEN · IN PROGRESS · RESOLVED (kept for traceability).

| ID | Issue | Evidence | Next action | Blocks | Status |
|---|---|---|---|---|---|
| OI-1 | MAI bolt-circle diameters, thread sizes and pilot diameters are ASSUMED; RobStride STEP models and dimensioned outlines exist (manual §1.1, image only) | `actuators/modular_actuator_interface.md` | dimension RS00/02/03/04/06 STEP files → update `tools/cad/params.py` → rebuild CAD (fully parametric) | ordering CNC parts | OPEN |
| OI-2 | **Actuator margins at the fast gait (0.79 m/s)** with the CAD masses (33.6 kg): hip yaw 37.0 N·m raw dynamic peak vs RS06 36 N·m (needs 55.5 with the 1.5× policy); hip roll 43.1 vs RS03 60 (needs 64.6); hip pitch 80.0 vs RS04 120 (exactly at policy). Slow/nominal (≤ 0.52 m/s), turning and squat meet every margin (nominal: hip yaw 20.8, hip roll 39.5, hip pitch 49.2, knee 49.6 N·m). Ankle motor static worst case 31.2 N·m = 1.15× (policy 1.25×). MuJoCo walking on the CAD model: fast gait hip yaw at 100 % of peak (clipped) but stable | `calculations/results/iter2_C_cad_masses/requirements.yaml`, `summary.json`; `verification/mujoco_walking.json` | decide: (a) cap the walking speed at ≈ 0.6 m/s with the current actuators (controller/RL command limit), or (b) RS03 at the hip yaw (60 N·m, +0.26 kg/leg, hip-yaw housing class M→L in the CAD) for 0.8 m/s; plus mass reduction (pockets, phase 3) | actuator order | OPEN (decision) |
| OI-3 | Ankle corner (plantarflexion + inversion): rod B touches motor B housing | `verification/ankle_workspace_L.json` (162-pose SolidWorks sweep, 92 % of the box free) | coupled pitch/roll polygon in `design_point.yaml` → `joint_map.yaml` `coupled_limits`, enforced by the Jetson controller | — | RESOLVED |
| OI-4 | Structural adequacy of the structure | `calculations/results/structural/report.md` | printed PA-CF failed everywhere (legs SF 0.07–0.90, upper body 0.14–1.23 static) → 6061/7075 plate. Final CAD: hip yaw 7075 1.77/1.58, hip roll 2.76/3.80, thigh 2.31/2.78, shin 3.37/4.17, foot 3.50/2.03 (lightening hole moved off the clevis; was 1.48 fatigue), pelvis 2.90/3.44, crank 14.4/24.7, torso 4.44/3.59, shoulder pitch 2.17/1.75, shoulder roll 2.05/1.66, forearm 3.27/2.65; upper arm redesigned (10/8 mm + gussets: 5.99/4.84 on the final CAD; 6 mm failed 1.34/1.08) | bolted-joint rules (OI-17); prototype strain check | release of CNC parts | RESOLVED (design) |
| OI-5 | Part materials inside SolidWorks | `tools/cad/make_manufacturing.py` | every machined part now carries its SolidWorks library material (6061-T6 (SS), 7075-T6 (SN), AISI 1045) and linked MATERIAL/WEIGHT properties; printed parts (PA-CF, PETG-CF) have no library material; simulation keeps the documented densities | custom PA-CF/PETG-CF materials; weigh the first parts and update densities | accurate inertias | IN PROGRESS |
| OI-6 | Knee flexion limited to 120° (target ≥ 135° for get-up/kneel) | CAD verification | V2 knee-at-hip linkage study | get-up behaviours | OPEN |
| OI-7 | Leg–leg contact beyond ≈ 8° hip adduction with the other leg vertical | `verification/robot_motion_verification.json` (single_leg_overshift: 14° stance adduction + adducted swing leg → 13 contacts; single_leg_T_pose at 8° with the swing leg abducted 6° is clear) | self-collision constraint in controller/RL; consider 0.21 m hip spacing | one-leg balance | OPEN |
| OI-8 | Upper body (torso, battery bay, electronics, arms, head) | `verification/upper_motion_verification.json` (36/36 exact, 36/36 collision-free), `verification/robot_motion_verification.json` (13/13 exact, 11/11 collision-free within the documented limits), FEA above | wiring routing through the joints and covers (phase 3) | — | RESOLVED (CAD, verification, FEA) |
| OI-9 | Actuator rotor inertia / phase resistance not published — armature and copper-loss values ESTIMATED | `actuators/actuator_catalog.yaml` | bench sys-ID (torque–speed, thermal, friction) | accurate sim + runtime | OPEN |
| OI-10 | Hole-pattern clocking between actuator envelopes and brackets not aligned after the connector clocking | CAD | align patterns once OI-1 values are known | drawings | OPEN |
| OI-11 | Manufacturing drawings | `manufacturing/index.md` | 24 STEP files, 14 A3 PDF drawings (views, material, process, qty, tolerance, envelope, SolidWorks mass, hole list; STEP = dimensional master), 24 PETG fit-check STLs, 3 print STLs | re-export after OI-1 | ordering | RESOLVED |
| OI-12 | CNC/laser prices ESTIMATED (no public per-part pricing in India); more CNC parts after OI-4 | `bom/master_bom.csv` | Robocon CNC / Makenica / local laser-shop quotes with the DXF/STEP set | cost accuracy | OPEN |
| OI-13 | GitHub remote `nickthelegend/thenar-jx1` does not exist and the available token cannot create repositories | `git push` → "Repository not found" | owner creates the empty private repo (or grants Administration: write) → `git push -u origin main` | publishing | OPEN (BLOCKER for push) |
| OI-14 | RobStride output-bearing load ratings (axial/radial/tilting moment) are not published; the hip-yaw RS06 carries the whole leg's pitch/roll moments as a tilting moment (up to ≈ 130 N·m in LC1, ≈ 64 N·m walking) | manual rev. 260713 (no bearing table); `calculations/results/structural/report.md` hand calcs | ask RobStride for bearing ratings; design provision for a yaw support bearing (thin-section 6816/crossed-roller between pelvis and yaw bracket) | hip-yaw release | OPEN |
| OI-15 | Mass growth from the aluminium structure changes the dynamics | `calculations/results/iter2_C_cad_masses/` (33.61 kg total, upper body 12.0 kg) | iteration-2 leg analysis done → result in OI-2; power budget: 92 W standing, 285 W walking 0.52 m/s, runtime 1.3 h walking / 4.1 h standing | — | RESOLVED (→ OI-2) |
| OI-16 | Shoulder-pitch CAD mate window limited to −115…+60° (SolidWorks angle-mate windows must lie in 0–180°); the −170° overhead range is not CAD-verified | `tools/cad/upper_kinematics.py` `CAD_WINDOW` | second limit-mate configuration for the overhead range, or verify in MuJoCo with CAD collision meshes | overhead reach | OPEN |
| OI-17 | Bolted plate joints (hip-roll bracket, shin, pelvis box) are modelled as monolithic in the FEA; joint slip/efficiency not analysed | FEA method note | dowel + preload rules per joint; bolt-group calcs per joint; prototype strain check | release of plate parts | OPEN |
| OI-18 | Arms hanging straight leave ≈ 27 mm between gripper and thigh plate; 30° hip abduction closes it | `verification/robot_motion_verification.json` (abduction_arms_down: contact, kept as a negative test; abduction_arms_relaxed with shoulder roll 8° is clear) | controller constraint (shoulder roll ≥ 8° while walking / abducting) — the MuJoCo walking posture and the RL task use 8° | — | OPEN (constraint) |
| OI-19 | Drawings and CAD come from a SolidWorks **Educational** licence: every PDF carries "For Instructional Use Only" and the licence restricts commercial use | `manufacturing/drawings/*.pdf` | fine for a personal/educational build; for commercial manufacture re-export from a commercial licence or rebuild from the STEP files in another CAD | commercial use | OPEN (owner decision) |
| OI-20 | Parallel ankle is driven as two per-motor MIT loops in the hubs, so the joint-space pitch/roll stiffness ratio is fixed by the linkage (≈ 1.27, J ≈ [[0.8, 0.9], [0.8, −0.9]]); a 60/40 joint-space PD is not realisable | `ros2_ws/src/jx1_hw` (HIL twin), `firmware/hub/jx1_hub` | RL task now uses the realisable 51.2/64.8 N·m/rad (motor kp 40, kd 1.5); optional firmware: add the coupling torques at 500 Hz for exact joint-space ankle PD | ankle control quality | OPEN (firmware option) |
| OI-21 | Ankle torque headroom: the interim RL policy's ankle pitch peaks at 87–100 % of the 46 N·m linkage capability in sim-to-sim; hub torque caps are 80 % of peak (e.g. hip yaw 28.8 of 36 N·m) | `rl/policies/jx1_walk_flat/sim2sim.json` | torque-limit penalty (85 % soft limit) in the fine-tune; bench-check ankle motor/linkage margin; related to OI-2 | aggressive gaits | OPEN |
| OI-22 | Isaac Lab task and the Isaac Sim ROS 2 bridge are UNVERIFIED: no Isaac Sim on the design PC (RTX 3050 6 GB is below Isaac Sim's recommended spec) | `simulation/isaac/isaaclab/`, `simulation/isaac/` | run on an Isaac-capable PC | Isaac workflow | OPEN |
| OI-23 | Sim-to-real gaps not modelled in training: RobStride MIT-mode latency and torque–speed curve, joint friction, IMU noise/latency, CAN timing (the hub's 20 ms target ramp is now modelled) | `rl/` | bench actuator system ID (see OI-9), then actuator model + latency in training; HIL twin in `ros2_ws/src/jx1_hw` | first hardware walking | OPEN |
| OI-24 | Hip-yaw bracket tails clash when both hips toe out: first contact at 21° each (symmetric), or 11° on one side with the other at its 45° limit (MuJoCo, CoACD hulls — conservative); toe-in never clashes | MuJoCo sweep 2026-09-24 (`simulation/mujoco/jx1.xml`) | coupled limit toe-out_L + toe-out_R ≤ 40° in the controller/RL task (walking and 15°/step turning stay far below); confirm in SolidWorks when the robot assembly is rebuilt; shorter bracket tail or 0.21 m hip spacing in V2 | aggressive turning | OPEN |
| OI-25 | `JX1_Robot.SLDASM` predates the upper-arm redesign: its rebuild + whole-robot re-verification was aborted to relieve system memory (commit near the 52 GB limit, C: full) | `verification/robot_motion_verification.json` (pre-redesign arms) | `build_robot_assembly.py` + `verify_robot_motion.py --images` in a fresh SolidWorks session with ≥ 12 GB commit headroom | — | OPEN |
| OI-26 | Fast-gait peak battery current ≈ 40 A (1682 W at 41.6 V, CAD masses) equals the JBD SP14S004 40 A BMS rating; the 13S2P 50S pack itself allows 50 A cont. | `calculations/results/iter2_C_cad_masses/power_budget.json` | set the BMS over-current delay for 100-ms peaks, or use the Daly 13S 60 A smart BMS (BOM alternative, ₹3,499); resolves with the ≈ 0.6 m/s speed cap of OI-2 | fast gait | OPEN |
